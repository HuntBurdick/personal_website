<% if item.errors.present? %>
  <div class="form-errors-wrapper">
    <h3>Found <%= item.errors.count %> Issues:</h3>
    <ul>
      <% item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
